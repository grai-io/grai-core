# About this directory

This directory will contain:
- `index.js`: the Webpack asset-relocator loader build.

It is generated by `ncc build src/asset-relocator.js`.

This directory is the only one published to npm.
